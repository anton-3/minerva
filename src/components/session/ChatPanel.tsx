// ChatPanel — text chat sidebar showing conversation transcript
// Also serves as text input fallback when mic is unavailable (FR-011)
// Owner: Person A (Session Architect)

"use client";

export function ChatPanel() {
  // TODO: Implement in Phase 3 (T031)
  // - Display conversation history from sessionStore
  // - Text input field for fallback when mic is denied
  // - Auto-scroll to latest message
  // - Show who is speaking (student vs tutor)

  return (
    <div className="flex flex-col bg-muted rounded-lg p-4 h-full">
      <p className="text-muted-foreground text-sm">Chat Panel — Not yet implemented</p>
    </div>
  );
}
