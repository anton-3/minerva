// SessionControls — mic toggle, session timer, end session button
// Owner: Person A (Session Architect)

"use client";

export function SessionControls() {
  // TODO: Implement in Phase 3 (T032)
  // - Mic on/off toggle
  // - Session timer (count up from start, warn at 8 min for HeyGen limit)
  // - End session button
  // - Connection status indicator

  return (
    <div className="flex items-center gap-4 p-4">
      <p className="text-muted-foreground text-sm">Session Controls — Not yet implemented</p>
    </div>
  );
}
